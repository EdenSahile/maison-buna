# Handoff — 2026-05-30 14:45

## Feature active

Migration React + Vite + Styled Components — en cours sur la branche `feature/react-migration`.

## Objectif de la session

Exécuter le plan d'implémentation `docs/superpowers/plans/2026-05-28-react-migration.md` tâche par tâche (18 tâches au total). Avancement pédagogique : l'utilisateur apprend React en même temps — expliquer le comportement et les concepts à chaque composant, pas ligne par ligne.

## Ce qui a été fait

### Planification (session précédente)
- `docs/superpowers/specs/2026-05-28-react-migration-design.md` — spec de migration validée
- `docs/superpowers/plans/2026-05-28-react-migration.md` — plan d'implémentation complet (18 tâches, code inline)

### Tasks exécutées cette session (Tasks 1 à 6)

**Task 1 — Bootstrap Vite**
- `client/` — projet Vite + React scaffoldé (`npm create vite@latest client -- --template react`)
- `client/vite.config.js` — proxy `/api` → `http://localhost:3000`
- `client/index.html` — titre Maison Buna + Google Fonts (Cormorant Garamond + Inter)
- `styled-components` installé dans `client/`
- Fichiers démo Vite supprimés (App.css, index.css, react.svg, vite.svg)

**Task 2 — Theme + GlobalStyle**
- `client/src/theme.js` — palette complète Maison Buna (brown, dark, cream, sand, accent, error…)
- `client/src/styles/GlobalStyle.js` — reset CSS global + noise texture + body styles
- `client/src/main.jsx` — ReactDOM.createRoot + GlobalStyle + App
- `client/src/App.jsx` — racine simple qui retourne `<DevisForm />`

**Task 3 — Reusable-ui : Button + SectionHead**
- `client/src/components/Reusable-ui/Button.jsx` — `PrimaryButton` + `GhostButton` (styled.button, pas de JSX)
- `client/src/components/Reusable-ui/SectionHead.jsx` — en-tête numérotée (num, title, step)

**Task 4 — Reusable-ui : Field**
- `client/src/components/Reusable-ui/Field.jsx` — wrapper label + children + erreur + hint
  - Exports : `StyledInput`, `StyledSelect`, `StyledTextarea` (partagent `baseInputStyles` via `css`)
  - Prop `$invalid` pour bordure rouge
  - ⚠️ 3 warnings ESLint (react-refresh/only-export-components) — non bloquants, à corriger si besoin

**Task 5 — Reusable-ui : ChoiceGrid**
- `client/src/components/Reusable-ui/ChoiceGrid.jsx` — grille radio/checkbox cards
  - Radio : valeur = string, onChange(string)
  - Checkbox : valeur = array, onChange(array)
  - `options.map()` avec `key={opt.value}`

**Task 6 — Utilitaires + tests**
- `client/src/utils/formUtils.js` — `validate`, `computeStepProgress`, `computeProgress`, `buildPayload`, `INITIAL_FORM_DATA`
- `client/src/utils/formUtils.test.js` — 15 tests Vitest, tous passent ✅
- `client/package.json` — script `"test": "vitest run"` ajouté
- `vitest` installé en devDependency

### Playground
- `client/.gitignore` — `src/playground/` ajouté (ignoré par git)
- `client/src/playground/` — dossier créé pour les exercices de l'utilisateur
- L'utilisateur a créé `app.jsx` et `main.jsx` dans le playground pour s'entraîner

## État du build

- Serveur Express (`node server.js`) : ✅ inchangé, fonctionne
- Vite dev server (`cd client && npm run dev`) : ✅ démarre sur :5173 (affiche une erreur React car DevisForm n'existe pas encore — normal)
- Tests (`cd client && npm test`) : ✅ 15/15 passent
- PDF (`npm run pdf:test`) : ✅ inchangé
- Emails (`npm run mail:test`) : ✅ inchangé
- CONTEXT.md : non mis à jour (migration React trackée dans le plan, pas dans CONTEXT.md)

## Blocages / échecs

RAS — toutes les tasks se sont exécutées sans blocage.

## Prochaines étapes

Continuer le plan `docs/superpowers/plans/2026-05-28-react-migration.md` à partir de la **Task 7**.

### Task 7 — BrandPanel (`client/src/components/BrandPanel.jsx`)
Sidebar gauche avec logo, headline, stepper dynamique.
- Props : `stepProgress` (boolean[4]), `audience` (string)
- `stepProgress.findIndex(x => !x)` → détermine l'étape active
- Stepper : clic sur une étape scrolle vers la section correspondante
- Responsive : sidebar cachée sur mobile (position sticky → relative)

### Task 8 — AudienceToggle (`client/src/components/AudienceToggle.jsx`)
Toggle entreprise / particulier avec icônes SVG.
- Props : `audience`, `onChange`

### Task 9 — SectionProfil (`client/src/components/SectionProfil.jsx`)
Section 01 — rendu conditionnel selon audience (entreprise vs particulier).

### Task 10 — SectionContact (`client/src/components/SectionContact.jsx`)
Section 02 — champs prenom, nom, email, telephone.

### Task 11 — SectionCommande (`client/src/components/SectionCommande.jsx`)
Section 03 — ChoiceGrid quantite (différent selon audience) + frequence + moutures.

### Task 12 — SummaryRail + SectionPrecisions
- `client/src/components/SummaryRail.jsx` — récap live
- `client/src/components/SectionPrecisions.jsx` — section 04

### Task 13 — SuccessView (`client/src/components/SuccessView.jsx`)
Vue post-envoi avec récapitulatif.

### Task 14 — DevisForm (`client/src/components/DevisForm.jsx`)
Composant racine — tout l'état (useState), submit, validation, assemblage.

### Task 15 — Fix `routes/devis.js`
setImmediate : répondre immédiatement, PDF + emails en arrière-plan.

### Task 16 — `server.js`
Servir `client/dist/` en production via express.static.

### Task 17 — Root `package.json`
Scripts mis à jour (dev, build, test).

### Task 18 — Test end-to-end
Vérification navigateur : parcours entreprise + particulier + validation.

## Notes importantes pour la prochaine session

- **Style d'apprentissage :** L'utilisateur apprend React. Expliquer le **comportement** (state, props, JSX) — pas ligne par ligne. Il pose des questions si besoin, on valide et on avance.
- **Playground :** `client/src/playground/` — dossier gitignore où l'utilisateur s'entraîne. Ne jamais toucher.
- **Architecture décidée :** Vite standalone + JS/JSX + useState + scroll unique (1:1 migration) + Styled Components
- **formData shape** (défini dans `INITIAL_FORM_DATA`) :
  ```js
  { societe, secteur, collaborateurs, adresse, codepostal, ville,
    prenom, nom, email, telephone, quantite, frequence, moutures[], message }
  ```
- **onChange signature** dans tous les composants : `(field: string, value: string | string[]) => void`
- **`quantite`** est un champ unifié (remplace l'ancien `quantite` + `quantiteP`) — remis à `''` quand audience change
